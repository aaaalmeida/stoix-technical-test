{
  "name": "root",
  "private": true,
  "scripts": {
    "start:backend": "npx lerna run start:dev --scope backend"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^9.0.0"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
